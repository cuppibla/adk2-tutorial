# Copy to .env and paste your key. Get one at https://aistudio.google.com/apikey
# Set ONLY this one (setting both GOOGLE_API_KEY and GEMINI_API_KEY makes ADK
# print a "both are set" notice on every run).
GOOGLE_API_KEY=your-key-here
